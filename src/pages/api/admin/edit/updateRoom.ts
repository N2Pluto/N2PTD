// this is /api/admin/update/updateRoom.ts


import supabase from 'src/libs/supabase'
import middleware from '../middleware'


const handler = async (req: any, res: any) => {
  middleware(req, res, async () => {

  })
}

export default handler
